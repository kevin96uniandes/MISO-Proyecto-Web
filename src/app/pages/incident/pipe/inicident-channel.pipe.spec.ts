import { InicidentChannelPipe } from './inicident-channel.pipe';

describe('InicidentChannelPipe', () => {
  it('create an instance', () => {
    const pipe = new InicidentChannelPipe();
    expect(pipe).toBeTruthy();
  });
});
