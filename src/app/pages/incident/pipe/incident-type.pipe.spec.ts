import { IncidentTypePipe } from './incident-type.pipe';

describe('IncidentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new IncidentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
